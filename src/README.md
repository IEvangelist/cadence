# src

.NET services orchestrated by Aspire. Uses Central Package Management
(`Directory.Packages.props`) with exact, pinned versions and lockfile-backed
restore. SDK pinned via root `global.json`.

Planned projects: `*.AppHost`, `*.ServiceDefaults`, `*.Api`, `*.AiService`
(Python/ONNX), `*.Separation` (Demucs/ONNX).

> Status: to be scaffolded (`dotnet new aspire-*`).

# apps/web

TypeScript SPA (Vite + React) — the entire user interface. Scaffolded in Phase 0
with the latest stable Vite + React, exact-pinned versions, and a committed
`package-lock.json`. Owned by the **Audio/Composer** and **Platform** squads.

> Status: to be scaffolded (`npm create vite@latest`).

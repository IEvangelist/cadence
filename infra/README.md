# infra

Deployment and infrastructure-as-code. Target: **Azure Container Apps** via `azd`
(Aspire-native), with Postgres, Blob Storage, and Redis. Container base images are
pinned by digest; provider/module versions are pinned. Owned by the
**DevOps/Release** squad.

> Status: to be generated (`azd init` / `aspire publish`).
